/**
 * Core model and business rules.
 *
 * <p>This package is the innermost layer: it depends on nothing else in the
 * project and carries no framework types. Everything here is plain Java, so the
 * rules can be tested without starting a Spring context.
 */
package dev.inboxpilot.domain;
